name: Flutter Build

on:
  push:
    branches:
      - master

jobs:
  # build-android:
  #   name: Build Android APK
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Set up Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.32.1'

  #     - name: Install dependencies
  #       run: flutter pub get

  #     - name: Build APK
  #       run: flutter build apk --release

  #     - name: Upload APK to Release
  #       uses: softprops/action-gh-release@v1
  #       with:
  #         tag_name: v1.0.0  # <-- Modifie ce tag dynamiquement si nécessaire
  #         name: Android Release v1.0.0
  #         files: build/app/outputs/flutter-apk/app-release.apk
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # build-web:
  #   name: Build Web
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Set up Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: '3.32.1'

  #     - name: Install dependencies
  #       run: flutter pub get

  #     - name: Build Web
  #       run: flutter build web --base-href /lupine/

  #     - name: Deploy to GitHub Pages
  #       uses: peaceiris/actions-gh-pages@v3
  #       with:
  #         github_token: ${{ secrets.GITHUB_TOKEN }}
  #         publish_dir: ./build/web

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.1'

      - name: Install system dependencies for Linux build
        run: |
          sudo apt-get update -y && sudo apt-get upgrade -y;
          sudo apt-get install -y curl git unzip xz-utils zip libglu1-mesa
          sudo apt-get install \
            clang cmake git \
            ninja-build pkg-config \
            libgtk-3-dev liblzma-dev \
            libstdc++-12-dev

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Enable Linux desktop support
        run: flutter config --enable-linux-desktop

      - name: Build Linux
        run: flutter build linux

      - name: Upload Linux build to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v1.0.0  # ou une version dynamique
          name: Linux Release v1.0.0
          files: build/linux/x64/release/bundle/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
